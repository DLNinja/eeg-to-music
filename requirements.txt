# EEG-to-Music Cross-Platform Requirements
# These requirements handle both Windows and Linux automatically.

# Core numeric and analysis
numpy>=2.0.0
scipy>=1.10.0
matplotlib>=3.7.0
pandas>=2.0.0
scikit-learn>=1.2.0
scikit-image>=0.20.0

# Deep Learning (PyTorch)
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# EEG Signal Processing
mne>=1.4.0
biosppy>=2.0.0
neurokit2>=0.2.0

# GUI Framework
PyQt5>=5.15.0

# Music and MIDI
mido>=1.2.0
pyfluidsynth>=1.3.0
pygame>=2.0.0

# Dependencies found in original setup
h5py>=3.0.0
opencv-python>=4.7.0
pillow>=9.5.0
imageio>=2.28.0
tqdm>=4.65.0
requests>=2.28.0
PyYAML>=6.0.0

# Linux-Specific Packages (ignored on Windows)
triton; sys_platform == 'linux'
pexpect; sys_platform == 'linux'
ptyprocess; sys_platform == 'linux'
nvidia-cublas-cu12; sys_platform == 'linux'
nvidia-cuda-cupti-cu12; sys_platform == 'linux'
nvidia-cuda-nvrtc-cu12; sys_platform == 'linux'
nvidia-cuda-runtime-cu12; sys_platform == 'linux'
nvidia-cudnn-cu12; sys_platform == 'linux'
nvidia-cufft-cu12; sys_platform == 'linux'
nvidia-cufile-cu12; sys_platform == 'linux'
nvidia-curand-cu12; sys_platform == 'linux'
nvidia-cusolver-cu12; sys_platform == 'linux'
nvidia-cusparse-cu12; sys_platform == 'linux'
nvidia-nccl-cu12; sys_platform == 'linux'
nvidia-nvjitlink-cu12; sys_platform == 'linux'
nvidia-nvtx-cu12; sys_platform == 'linux'
